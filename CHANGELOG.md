# Changelog

## v1.3.1 (2026-06-10)

### 修复
- **恐惧贪婪圆圈溢出**：偏离信号文字从圆圈内部移到下方独立显示，避免 72px 圆内文字溢出
- **操作建议信号格式**：`→3🟢1🔴` 改为 `[多3空1]`，每个维度标注 `(多)/(空)`，更直观可读
- **WeasyPrint 模板语法修复**：`{{#if warning}}` 改为 pystache 兼容的 `{{#warning}}`
- 同步更新 SKILL.md、investment-rules.md 中的格式定义和示例

## v1.3.0 (2026-05-21)

### 从后视镜到前瞻：多维信号 + 反共识 + 大跌加仓 + 止盈后加回

**搜索体系重构**（4次→5次）：
- 搜索 1：市场行情（PE分位、涨跌幅、北向资金）
- 搜索 2：上游基本面（芯片/HBM/光模块 产能 投产 技术突破 EPS 盈利增速）
- 搜索 3：中下游基本面（大模型API调用量 机器人智驾 渗透率 政策 商业化）
- 搜索 4：基金业绩排名
- 搜索 5：情绪+隔夜（隔夜美股 纳斯达克 AI 期货 反弹 市场情绪）

**四维信号矩阵（三层各自设计）**：
- 上游：PE分位 + EPS增速 + 供给紧张度 + 情绪偏离（核心矛盾：结构性短缺）
- 中游：PE分位 + 增长(API/用户) + 竞争格局 + 情绪偏离（核心矛盾：变现能力）
- 下游：PE分位 + 渗透率 + 产业趋势 + 情绪偏离（核心矛盾：渗透率拐点）
- PE分位80-95%不单独触发止盈，必须结合盈利和供给

**反共识规则（方案B：相对偏离）**：
- 恐惧贪婪偏离近20日均值>15点触发反共识校准
- 贪婪骤升+基本面撑不住=泡沫减仓；贪婪骤升+基本面撑得住=合理持有
- 恐惧骤降+基本面没坏=错杀加仓；恐惧骤降+基本面真坏=回避

**新增大跌加仓分析（Step 1.6）**：
- 赛道跌>3%触发四步判断：查原因→查隔夜→查期货→下判断

**止盈后加回条件**：
- 每条止盈建议附带加回条件

**建议输出升级**：
- 每条建议标注四维信号矩阵 + 加回条件
- 禁止后视镜，要求前瞻判断+条件式建议

## v1.2.1 (2026-05-21)

### 修复
- 搜索词时间变量：使用 `$TODAY` 和 `$YEAR` 变量，确保每次运行使用当日日期
- SKILL.md 文档：明确标注变量替换规则，避免使用固定日期

## v1.2.0 (2026-05-20)

### 重大更新
- **纯动态基金发现**：移除固定基金基线，每次运行从搜索结果动态提取
- **Step 1.5 新增**：基金发现→分层映射→业绩排序→持仓验证

### 模板与渲染
- WeasyPrint 兼容：`render_pdf_simple.py` 自动提取 DATA 渲染
- 修复 Mustache 三花括号正则（`{{{advice}}}` 多出 `}` 问题）
- 修复反义条件 `{{^var}}` 未处理（暂无推荐不显示）
- 修复布尔值 `{{#is_new}}` 不渲染问题
- 涨跌颜色统一：涨绿跌红（去掉蓝色）
- 恐惧贪婪圆环统一纯色（去掉 conic-gradient）
- 新增 `[新上]` 标签 + `(近1年)` 收益周期标注
- 新增 "暂无基金推荐" 空状态处理
- 初始 DATA 清空默认值

### 其他
- 移除 market-baseline.md（不再依赖固定基线）
- emoji 替换为符号避免 PDF 方块

## v1.0.0 (2026-05-18)

- 初始版本
- AI板块投资分析 PDF 报告生成
- 产业链分层（上游/中游/下游）
- 择时信号规则
- 基金推荐基线
