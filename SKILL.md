---
name: ai-sector-investor
version: 1.3.1
description: "AI板块投资分析与PDF报告生成。当用户需要分析AI板块投资、查看板块轮动、获取基金推荐、生成投资报告时使用。最终输出为PDF格式。"
---

# AI板块投资分析师

为用户提供AI板块（人工智能产业链）的投资分析服务，最终生成**PDF格式的投资报告**。

**核心原则**：不写后视镜（"昨天跌了"），写前瞻判断（"隔夜反弹+X信号，今天怎么做"）。别人贪婪我恐惧，别人恐惧我贪婪——但每条反共识判断必须引用搜索到的具体数据。

## 触发场景

- 用户提到"AI板块"、"人工智能投资"、"半导体基金"、"机器人ETF"等关键词
- 用户需要分析AI产业链上下游轮动
- 用户需要高位止盈/低位建仓的择时建议
- 用户要求生成投资报告、播报、分析

## 执行流程（固定输出PDF）

### Step 1：数据获取（WebSearch）

以下搜索每次运行时将 `$TODAY` 替换为**当日日期**（格式：YYYY年MM月DD日，如 2026年05月20日）。共 **5 次搜索**：

1. **市场行情**：`"$TODAY AI板块 人工智能 半导体 机器人 算力 涨跌幅 PE估值 北向资金"`
2. **上游基本面**：`"$TODAY 半导体 芯片 HBM 光模块 服务器 产能 投产 技术突破 EPS 盈利增速"`
3. **中游+下游基本面**：`"$TODAY 大模型 API调用量 机器人 智驾 渗透率 政策 商业化 用户增长"`
4. **基金业绩排名**：`"$TODAY AI人工智能基金 业绩排名 近一年收益率 site:fund.eastmoney.com OR site:finance.sina.com.cn"`
5. **情绪+隔夜**：`"$TODAY 隔夜 美股 纳斯达克 AI 期货 反弹 市场情绪"`

> **重要**：`$TODAY` 必须替换为运行当天的日期，不要使用固定日期

> **搜索来源限制**：优先使用权威金融网站
> - `site:fund.eastmoney.com` - 天天基金网
> - `site:finance.eastmoney.com` - 东方财富
> - `site:finance.sina.com.cn` - 新浪财经
> - `site:cs.com.cn` - 中国证券报
> - `site:cnstock.com` - 上海证券报

### Step 1.5：基金动态发现与分层

**纯动态搜索，不依赖固定基线**。从 Step 1 第 4 次搜索（基金业绩排名）提取当日表现最佳的 AI 主题基金，按产业链层级自动分类。

#### 1.5a 基金发现（用第 4 次搜索的结果）

| 操作 | 说明 |
|------|------|
| **提取基金** | 从搜索结果提取基金名称、代码、近一年收益率 |
| **去重排序** | 按收益率排序，同名基金保留业绩最优者 |
| **失败处理** | 搜索失败或无结果时，该层级显示"暂无推荐" |

#### 1.5b 基金分层映射

根据基金名称/投资方向关键词自动分类：

| 层级 | 关键词 | 示例 |
|------|--------|------|
| **上游** | 半导体、芯片、算力、光模块、存储、服务器、GPU | 芯片ETF、半导体基金 |
| **中游** | 大模型、平台、云计算、软件、MaaS、开发 | AI平台基金、云计算ETF |
| **下游** | 应用、机器人、智能驾驶、AI+、传媒、医疗、金融 | 机器人ETF、AI应用基金 |

> **分类规则**：匹配关键词即归入对应层级；同时匹配多个层级时，按上游>中游>下游优先级；未匹配默认归入下游。

#### 1.5c 数值更新

| 更新项 | 对比来源 | 操作 |
|--------|---------|------|
| **sectors 的 change / pe_pct** | 从第 1 次搜索的市场数据提取 | 逐赛道填入最新数值 |
| **indices 的 pe_pct** | 从第 1 次搜索的指数估值数据提取 | 填入 4 个指数的 PE 百分位 |
| **flow 的资金数据** | 从第 1 次搜索的北向/主力资金数据提取 | 填入 north_1d/5d/20d 和 main_1d |
| **fear_greed** | 从第 1、5 次搜索提取或自行计算 | 填入 value 和 zone |
| **恐惧贪婪偏离** | 近 20 日均值 vs 今日值 | 填入 fear_greed.bias（±点数） |
| **funds 业绩** | 从第 4 次搜索提取 | 填入 return_1y（近一年收益率%） |
| **EPS 增速** | 从第 2 次搜索提取 | 填入 sectors[].eps_growth（同比%） |
| **供给/竞争信号** | 从第 2、3 次搜索提取 | 填入 sectors[].supply_signal（强势/中性/松动） |
| **渗透/增长信号** | 从第 3 次搜索提取 | 填入 sectors[].growth_signal（加速/平稳/放缓） |

> **数据来源**：Step 1 全部 5 次搜索结果 + Step 1.5 动态发现

#### 1.5d 持仓验证

对 Top 基金（每层级前2名）二次搜索持仓，确认分层准确性：
- 搜索词示例：`"{基金名称} 持仓 重仓股 $YEAR"`（`$YEAR` 替换为当前年份，如 2026年）
- 如果实际持仓与名称关键词分层矛盾，调整到正确层级
- 如果搜索失败，保持按名称关键词分层

### Step 1.6：大跌加仓分析

**不要只报"昨天跌了"——要判断今天是该加仓还是观望。**

当日有赛道单日跌超 3% 时，触发以下分析：

| 步骤 | 操作 | 数据来源 |
|------|------|---------|
| ① 查原因 | 跌因是技术回调还是基本面利空？ | 第 2、3 次搜索 |
| ② 查隔夜 | 隔夜美股/海外 AI 板块是否反弹？ | 第 5 次搜索 |
| ③ 查期货 | 当日股指期货开盘方向？ | 第 5 次搜索 |
| ④ 下判断 | 综合给出加仓/观望/减仓建议 | `references/investment-rules.md` |

**判断矩阵**：

| 跌因 | 隔夜海外 | 期货 | 判断 | 操作 |
|------|---------|------|------|------|
| 技术回调 | 反弹 | 走高 | 🟢 短线加仓机会 | 可加仓 10-20% |
| 技术回调 | 持平 | 持平 | 🟡 观望 | 等企稳 |
| 基本面利空 | — | — | 🔴 真正利空 | 减仓/观望 |
| 系统性大跌 | 大跌 | 大跌 | 🔴 系统性风险 | 减仓避险 |

### Step 1.7：多维信号计分

**不再用单一 PE 分位判断止盈，改为四维打分，且每个层级权重不同。**

详见 `references/investment-rules.md`。核心逻辑：

| 层级 | 四维信号 | 核心矛盾 |
|------|---------|---------|
| **上游** | PE分位 + EPS增速 + 供给紧张度 + 情绪偏离 | 结构性短缺 |
| **中游** | PE分位 + 增长(API/用户) + 竞争格局 + 情绪偏离 | 变现能力 |
| **下游** | PE分位 + 渗透率 + 产业趋势/政策 + 情绪偏离 | 渗透率拐点 |

**计分规则**：每维度判断为看多或看空
- 4多 = 重仓、3多 = 可加仓、2多2空 = 观望、3空 = 止盈30%、4空 = 清仓

**层级特殊规则**：
- 上游：即使 3空，只要"供给"看多，止盈幅度减半
- 中游："增长"和"竞争"同时看空时，无论估值都应减仓
- 下游：渗透率权重最高，政策是最大催化剂

> 每维度信号必须从搜索结果中提取，不能无依据判断。

### Step 1.8：反共识校准

**核心：别人贪婪我恐惧，别人恐惧我贪婪——恐惧贪婪偏离>15点 + 基本面数据验证。**

| 场景 | 条件 | 判断 |
|------|------|------|
| 贪婪骤升（偏离>+15） | + 基本面撑得住（EPS>30%/渗透>预期） | 🟢 持有，不追 |
| 贪婪骤升（偏离>+15） | + 基本面撑不住（EPS放缓/渗透停滞） | 🔴 减仓，泡沫 |
| 恐惧骤降（偏离>-15） | + 基本面没坏（EPS>30%/渗透>预期） | 🟢 加仓，错杀 |
| 恐惧骤降（偏离>-15） | + 基本面真坏（EPS负/安全事故） | 🔴 回避，真恶化 |

> 每一条反共识判断必须引用搜索到的具体数据，如："恐惧贪婪从均值72骤降至52（-20点）+ EPS增长35% + 产能满载→判断为过度恐慌，可加仓"。

### Step 2：生成HTML报告

使用 `templates/ai-sector-briefing-playwright.html` 模板（**首选方案**）。该模板为**纯数据驱动**：将下方 JSON 注入模板顶部的 `DATA` 对象，打开即可渲染。无需修改 HTML 结构。

> **两套模板说明**：
> - `ai-sector-briefing-playwright.html` - 完整效果版，支持 `conic-gradient` 渐变仪表盘、动态渲染
> - `ai-sector-briefing-weasyprint.html` - 兼容降级版，纯色替代渐变，纯静态 HTML 无需 JavaScript

```json
{
  "date": "YYYY年MM月DD日 周X",
  "signal_level": "warn",
  "signal_text": "🟡 中性偏谨慎",
  "indices": [
    {"name": "沪深300", "pe_pct": 62},
    {"name": "中证500", "pe_pct": 48},
    {"name": "科创50", "pe_pct": 95},
    {"name": "创业板指", "pe_pct": 55}
  ],
  "fear_greed": {"value": 62, "zone": "偏贪婪", "bias": -5},
  "flow": {"north_1d": 12.5, "north_5d": 38.2, "north_20d": 156.8, "main_1d": -8.3},
  "sectors": [
    {"name": "AI训练芯片", "layer": "up", "change": 2.1, "pe_pct": 88, "eps_growth": 45, "supply_signal": "紧缺"},
    {"name": "推理芯片/ASIC", "layer": "up", "change": 3.5, "pe_pct": 72, "eps_growth": 30, "supply_signal": "紧缺"}
  ],
  "funds": [
    {"name": "广发远见智选混合A", "code": "016873", "type": "主动型", "layer": "up", "return_1y": 111.2},
    {"name": "富国创新科技混合A", "code": "002692", "type": "主动型", "layer": "mid", "return_1y": 247.3}
  ],
  "news": [
    {"text": "新闻内容", "impact": "利好/利空/中性"}
  ],
  "advice_title": "核心：上游保留底仓，大跌即加仓机会",
  "advice": "🔹 <b>AI芯片ETF</b>：PE88%(空)+EPS45%(多)+供给紧缺(多)+情绪中性→[多3空1]→可持有，减仓20%保留底仓<br>🔹 <b>机器人ETF</b>：PE45%(多)+渗透超预期(多)+政策利好(多)+情绪中性→[多4空0]→维持布局<br>🔹 <b>操作策略</b>：大跌先查原因→技术回调+隔夜反弹=加仓信号；基本面利空=真减仓",
  "footer_text": "数据来源：公开市场信息 · YYYY.MM.DD"
}
```

**字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| `sectors[].change` | number | 昨日涨跌幅(%) |
| `sectors[].pe_pct` | number | PE 近 5 年百分位(0-100) |
| `sectors[].layer` | string | 产业链层级：`up`/`mid`/`down` |
| `sectors[].eps_growth` | number/null | EPS 同比增速(%)，上游/中游使用 |
| `sectors[].supply_signal` | string/null | 供给信号：紧缺/中性/松动，上游专用 |
| `sectors[].growth_signal` | string/null | 增长信号：加速/平稳/放缓，中下游专用 |
| `flow.north_1d` | number | 北向资金近 1 日净买入（亿元） |
| `flow.main_1d` | number | 主力资金近 1 日净买入（亿元） |
| `fear_greed.value` | number | 恐惧贪婪指数数值(0-100) |
| `fear_greed.bias` | number | 恐惧贪婪偏离近20日均值的点数(±) |
| `news[].impact` | string | 对板块影响：`利好`/`利空`/`中性` |

**数据填充规则**：
- 所有数值从 Step 1 全部 5 次搜索 + Step 1.5 验证后的数据提取
- `signal_level`：warn/danger/safe，综合四维信号推算
- `sectors` 和 `funds` 的 `layer` 取值为 `up`/`mid`/`down`
- `fear_greed.zone`：0-25极度恐惧 / 25-50中性 / 50-75偏贪婪 / 75-100极度贪婪
- `fear_greed.bias`：当日值 - 近20日均值，正数=情绪升温，负数=降温
- `eps_growth` / `supply_signal` / `growth_signal`：从 Step 1 第 2、3 次搜索提取

**建议生成规则**：

| 规则 | 说明 |
|------|------|
| **禁止后视镜** | ❌"昨天XX跌了X%" → ✅"隔夜反弹+期货走高，短线可加仓" |
| **四维引用** | 每条建议标注信号矩阵，如"PE88%(空)+EPS45%(多)+紧缺(多)+情绪中性→[多3空1]" |
| **上游特殊** | PE80-95%不喊止盈（结构性短缺），>95%止盈20-30%（非30-50%） |
| **反共识** | 恐惧贪婪偏离>15点 + 基本面校准 → 给出反向建议 |
| **加回条件** | 每条止盈建议附带加回条件："当PE回落至X%以下且EPS增速不变时加回" |
| **条件式** | ✅"如果期货开盘走强→加仓10%；如果冲高回落→观望" |

### Step 3：HTML渲染为PDF

**核心原则**：
1. **不要自己生成 static HTML**，必须使用 skill 目录下的模板
2. 优先 Playwright，失败时降级 WeasyPrint
3. WeasyPrint 降级时，`render_pdf_simple.py` 会自动从 Step 2 的动态 HTML 中提取 DATA，并使用 `ai-sector-briefing-weasyprint.html` 模板渲染

**首选 — Playwright**：

```bash
pip install playwright --break-system-packages
python -m playwright install chromium
python -m playwright install-deps chromium
python scripts/render_pdf.py <Step2生成的动态HTML路径> output/AI板块投资报告.pdf
```

**降级 — WeasyPrint**：

```bash
pip install weasyprint --break-system-packages
python scripts/render_pdf_simple.py <Step2生成的动态HTML路径> output/AI板块投资报告.pdf
```

> - 两个脚本都接收 Step 2 生成的同一个动态 HTML 文件，不需要手动切换模板
> - `render_pdf_simple.py` 会自动提取 DATA 并使用 `ai-sector-briefing-weasyprint.html` 模板

### Step 4：验证交付

1. **检查文件大小**：`ls -la output.pdf`，正常报告应 > 100KB，< 10KB 说明渲染失败
2. **失败处理**：检查 HTML 中 `const DATA` 是否正确替换；检查模板渲染日志是否有未替换的 `{{}}` 标记
3. **交付**：验证通过后提供 PDF 文件链接

## 报告结构（PDF固定格式）

```
┌─────────────────────────────────────┐
│  AI板块投资报告                      │
│  YYYY年MM月DD日  综合信号             │
├─────────────────────────────────────┤
│  一、估值温度计                       │
│  二、恐惧贪婪指数（含偏离信号）        │
│  三、AI产业链全景（多层多维信号）      │
│  四、资金流向                         │
│  五、择时信号面板                     │
│  六、推荐基金                         │
│  七、AI产业重要资讯                   │
│  八、今日操作建议（含反共识信号）      │
│  免责声明                             │
└─────────────────────────────────────┘
```

## 参考文档

- `references/investment-rules.md` - 三层四维信号矩阵、反共识规则、大跌加仓规则、季度配置

## 输出规范

- 最终输出：PDF格式投资报告
- 语言：中文
- 必须包含：数据来源标注 + 免责声明
- 每条建议必须包含：信号矩阵标注 + 加回条件（如有止盈）
